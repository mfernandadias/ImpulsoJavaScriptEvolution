var currentNumber = document.getElementById("currentNumber")

function increment() {
    currentNumber = currentNumber - 1;
    cuuentNumberWrapper.innerHTML = currentNumber;
}

function dec() {
    currentNumber = currentNumber - 1;
    currentNumberWrapper.innerHTML = currentNumber;
} 